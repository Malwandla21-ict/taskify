# Prompt for ChatGPT / design tool — Taskify dashboard mockup

Copy everything below into the tool:

---

Design a UI mockup for two screens of a university student marketplace web app called **Taskify** (University of Mpumalanga). Style: clean, modern SaaS/app design, card-based layout, generous white space, rounded corners (8-12px), soft shadows. Desktop web layout, left sidebar navigation.

**Brand colors:**
- Primary/navy: #0C1D4A
- Green (main accent/CTA color): #009B72
- Blue (secondary accent): #0072CE
- Gold (highlight/badge color): #F5B400
- Background: #F5F7FA
- Text: #333333, muted text: #687280
- Font: Poppins (clean geometric sans-serif)

**Screen 1 — First-time user onboarding modal (overlay on top of the dashboard, background dimmed):**
A centered modal/card, rounded corners, with:
- A large, bold tagline as the first thing you see, in navy or green: "[TAGLINE — e.g. 'Everything campus life needs, in one place.']"
- Below it, a short one-sentence subtext explaining Taskify is UMP's student marketplace for tasks, sales, rentals, and events
- Four small feature cards or icons in a row (or 2x2 grid on narrower screens), one each for: **Tasks** (clipboard icon), **Marketplace/Sales** (tag icon), **Equipment Rentals** (box icon), **Events** (calendar icon) — each with a 1-line description
- A prominent green "Get Started" button at the bottom, pill-shaped
- A small "skip" or close (X) option in the corner

**Screen 2 — Returning-user dashboard home (no modal, this is what's behind it):**
- Left sidebar with nav icons (Dashboard, Tasks, Marketplace, Equipment, Events, Messages, Profile)
- Top of main content: a wide hero banner section with:
  - The same short tagline, smaller/secondary placement (not as dominant as the modal version), in white text over a navy-to-green gradient background OR a background photo with a dark overlay
  - Below/beside the tagline: a rotating carousel/slideshow area showing 3 sample content cards side by side or one large auto-rotating card, mixing: an upcoming **Event** (with date badge), a featured **Task** (with a budget/price tag), and an **Equipment Rental** listing (with a photo) — each card clickable-looking with a category label chip (colored: green for tasks, blue for rentals, gold for events)
  - Small dot indicators under the carousel showing it rotates
- Below the hero: a "Featured Tasks" grid section, same visual card style as existing marketplace listings (image top, title, short description, price/tag row)

Render both screens as a single image or two side-by-side panels, labeled "New user — first visit" and "Returning user — dashboard home."

---

A few notes before you use it: I left the tagline as a placeholder — swap in whichever line you want, or if you want, I can draft 3-4 tagline options for you to pick from before you run this. Also, since this is just a visual reference for you and the stakeholder (not something I'll build from directly), don't worry about it being pixel-perfect — I'll build the real thing against your actual CSS regardless of how closely the mockup matches.
